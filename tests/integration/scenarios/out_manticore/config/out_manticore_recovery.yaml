service:
  flush: 0.2
  grace: 3
  log_level: info
  http_server: on
  http_port: ${FLUENT_BIT_HTTP_MONITORING_PORT}
  parsers_file: ${MANTICORE_PARSERS_FILE}

pipeline:
  inputs:
    - name: tail
      tag: manticore_recovery
      path: ${MANTICORE_INPUT_PATH}
      read_from_head: true
      exit_on_eof: ${MANTICORE_EXIT_ON_EOF}
      parser: recovery_json

  outputs:
    - name: manticore
      match: '*'
      host: 127.0.0.1
      port: ${MANTICORE_PORT}
      table: ${MANTICORE_TABLE}
      workers: 1
      single_chunk: true
      spool_path: ${MANTICORE_SPOOL_PATH}
